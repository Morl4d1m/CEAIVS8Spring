Theres really not much to it, install the dependencies (newest versions), set the flags for what you'd like to see and press run

The test script is integrated instead of in a separate file. Set the testing flag to 1 for testing.
